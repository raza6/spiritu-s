{
  "name": "spirituos",
  "version": "1.0.0",
  "description": "One german sentence dissected a day",
  "license": "ISC",
  "author": "raza6",
  "type": "commonjs",
  "main": "dist/app.js",
  "scripts": {
    "dev": "nodemon -e ts,handlebars --ignore dist/ --exec npm start",
    "start": "tsc && rm -rf dist/views && rm -rf dist/public && cp -r src/public/ dist/public/ && cp -r src/views/ dist/views/ && node dist/app.js",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "dependencies": {
    "express": "^4.17.1",
    "express-handlebars": "^8.0.1",
    "handlebars": "^4.7.8",
    "ky": "^1.14.1",
    "lowdb": "^7.0.1"
  },
  "devDependencies": {
    "@types/express": "^4.17.1",
    "nodemon": "^3.1.11",
    "typescript": "^5.9.3"
  }
}
